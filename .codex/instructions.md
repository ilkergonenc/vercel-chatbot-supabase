# Codex repository instructions

Read `AGENTS.md` first. It is the main agent entry point for this repository.

Use `.agents/context/`, `.agents/workflows/`, and `.agents/checklists/` for shared agent context when those files are relevant to the task. For repo-local skills, start with `.agents/context/skills-guide.md`. Repo inspection always wins over stale documentation.

Keep Codex-specific notes here short; put project documentation under `docs/` and shared agent guidance under `.agents/`.
